import SwiftUI

public extension Font {
    static let wmHeader = system(size: 28, weight: .bold)
    static let wmTitle = system(size: 18, weight: .bold)
    static let wmDescription = system(size: 16)
    static let wmSmallText = system(size: 12)
}
